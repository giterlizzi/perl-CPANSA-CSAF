name: Build CSAF

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:
    inputs:
      rebuild_all:
        description: 'Rebuild all CPANSA'
        type: boolean
        default: false
  schedule:
    - cron: '0 0 * * *'

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up perl
        uses: shogo82148/actions-setup-perl@v1
        with:
            perl-version: 5.36
            distribution: 'default'

      - name: Show Perl Version
        run: perl -v

      - name: Install Modules
        run: |
          cpanm -v
          cpanm --installdeps --with-develop --notest .

      - name: Build CPANSA
        env:
          CSAF_DEBUG: true
        run: |
          if [[ "${{ inputs.rebuild_all }}" == "true" ]]; then
            make rebuild
          else
            make build
          fi

      - name: Build ROLIE
        run: make rolie

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add csaf
          git commit -s -m "Sync with CPANSA" || echo "No changes to commit"
          git push

      - name: Generate pages
        env:
          CSAF_DEBUG: true
        run: make website

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
